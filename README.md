Generates a report for Checkmarx One

Run the PowerShell script to generate the data using
    .\CxOne-Report.ps1 [-scanDays] [-silentLogin -apiKey] [<CommonParameters>]

Update the location of the data files in the Excel and select fresh all
